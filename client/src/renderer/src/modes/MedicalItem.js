class MedicalItem {
  constructor(options) {
    this.id = options.id;
    this.name = options.name;
    this.price = options.price;
    this.type = options.type;
  }
}

export default MedicalItem;